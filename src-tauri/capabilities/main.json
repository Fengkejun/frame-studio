{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "main",
  "description": "Only the local main window may read application information.",
  "windows": ["main"],
  "permissions": ["allow-get-app-info"]
}
